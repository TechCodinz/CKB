name: Live Execution Twin Gate

on:
  pull_request:
    paths:
      - 'core/**'
      - 'mcp-server/**'
      - 'integrations/runtime/**'
      - 'docs/live-execution-twin.md'
      - '.github/workflows/live-execution-gate.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: live-execution-${{ github.ref }}
  cancel-in-progress: true

jobs:
  core-runtime-intelligence:
    name: Core runtime intelligence
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install stable Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2

      - name: Format check
        run: cargo fmt --all -- --check

      - name: Live Execution Twin tests
        run: cargo test -p ckb-core live_execution --no-fail-fast

      - name: Core compile check
        run: cargo check -p ckb-core

  core-runtime-verification:
    name: Core runtime compile and tests
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install stable Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo
        uses: Swatinem/rust-cache@v2

      - name: Live Execution Twin tests
        run: cargo test -p ckb-core live_execution --no-fail-fast

      - name: Core compile check
        run: cargo check -p ckb-core

  rustfmt-candidate:
    name: Canonical rustfmt candidate
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install stable Rust with rustfmt
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt

      - name: Apply canonical rustfmt in workspace
        run: cargo fmt --all

      - name: Upload formatted Live Execution source
        uses: actions/upload-artifact@v4
        with:
          name: live-execution-rustfmt
          path: core/src/analysis/live_execution.rs
          if-no-files-found: error
          retention-days: 1

  go-runtime-agent:
    name: Go runtime agent
    runs-on: ubuntu-latest
    timeout-minutes: 10
    defaults:
      run:
        working-directory: integrations/runtime/go
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.22.x'
          cache-dependency-path: integrations/runtime/go/go.mod

      - name: Format check
        run: test -z "$(gofmt -l .)"

      - name: Unit tests
        run: go test ./...

      - name: Vet
        run: go vet ./...
