# IntelliJ Platform already supplies Kotlin stdlib. Do not bundle a second copy.
kotlin.stdlib.default.dependency=false

# Keep build output deterministic and avoid unnecessary self-update network checks in CI.
org.jetbrains.intellij.platform.selfUpdateCheck=false
