{
    "mcpServers": {
        "ckb": {
            "command": "ckb-mcp-server",
            "args": [
                "--stdio"
            ],
            "env": {
                "CKB_PROJECT_PATH": "${workspaceFolder}"
            }
        }
    }
}